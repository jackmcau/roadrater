# Express server
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:5500,http://localhost:5500,https://roadrater-s51z.onrender.com
# Use commas to allow multiple origins for dev and production

# PostgreSQL connection
# Use POSTGRES_HOST/PGHOST for database host (defaults to localhost for production)
# For Docker Compose, set POSTGRES_HOST=db
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=roadrater
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Alternative: Use DATABASE_URL for connection string
# DATABASE_URL=postgres://username:password@host:port/database

# Authentication
JWT_SECRET=replace-me-with-a-secure-random-string
