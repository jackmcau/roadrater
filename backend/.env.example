# Express server
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000
# Use commas to allow multiple origins, e.g. http://localhost:3000,http://localhost:5173

# PostgreSQL connection (override host when not using docker-compose)
DB_HOST=db
DB_PORT=5432
POSTGRES_DB=roadrater
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Authentication
JWT_SECRET=replace-me-with-a-secure-random-string
