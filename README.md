# roadrater
1. Copy `ProjectSourceCode/backend/.env.example` to `ProjectSourceCode/backend/.env` and adjust values as needed.
2. Run backend unit/integration tests (optional):
	```bash
	cd ProjectSourceCode/backend && npm test
	```
3. Start the stack: `docker-compose up -d --build`