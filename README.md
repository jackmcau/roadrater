# roadrater
1. Copy `backend/.env.example` to `backend/.env` and adjust values as needed.
2. Run backend unit/integration tests (optional):
	```bash
	cd backend && npm test
	```
3. Start the stack: `docker-compose up -d --build`